{
  "name": "rpg-legend",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo do RPG Legend — cliente Next.js, servidor NestJS e engine compartilhada",
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.10.9",
    "typescript": "^5.9.3"
  }
}
