{
  "name": "@rpg-legend/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "//sideEffects": "A engine é pura: nenhum módulo mexe em global, DOM ou Node ao ser carregado (é a regra do CLAUDE.md que permite o Nest validar jogada com o mesmo código). Sem esta linha o bundler não pode provar isso e mantém o pacote inteiro — medido: /conta e /multiplayer baixavam os 62 KB do catálogo de itens e monstros só por importarem PET_ICONS.",
  "sideEffects": false,
  "types": "./dist/index.d.ts",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
